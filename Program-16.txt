#include <stdio.h>
#include <stdlib.h>
int main()
{
    int *ptr;
    ptr = (int *)malloc(5 * sizeof(int));
    if(ptr == NULL)
    {
        printf("Memory not allocated");
        return 0;
    }
    printf("Memory allocated successfully");
    free(ptr);
    return 0;
}