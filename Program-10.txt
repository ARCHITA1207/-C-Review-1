#include<stdio.h>
int main()
{
    int n,i,rev=0,rem;
    scanf("%d",&n);
    while(n!=0)
    {
        for(i=0;i<n;i++)
        {
            rem=n%10;
            rev=rev*10+rem;
            n=n/10;
        }
    }
    printf("Reverse integer ->%d",rev);
    return 0;
}
